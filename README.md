# NeoForge
100% safe AI terminal assistant for Android (Termux). Only read-only commands allowed (ls, cat, grep, find, etc). Impossible to rm, sudo, cd, pip, wget anything. Zero escape, bank-level filtering. It is designed to run smoothly on phones. Made in India with love on a $200 device.
